const heading = document.getElementsByTagName( 'h1' );
const main = document.getElementsByClassName("main");

// .innerText is used to get the text inside an HTML element and those text values are stored in a string and they visiable only screen.

// .innerContent it is use to get text form element and get those text he his blocked from display none also

// .innerHTML  will give the full content of that tag includeing tags also if we use tags in inner html like (<p> this is paragraph <span>test tag for innerHTML</span></p>) 